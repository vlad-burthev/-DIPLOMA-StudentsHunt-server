-- DROP TABLE roles;

CREATE TABLE IF NOT EXISTS roles(
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    role VARCHAR(50) NOT NULL CHECK (
        role IN('STUDENT', 'ADMIN', 'SUPERADMIN', 'COMPANY', 'UNIVERSITY', 'RECRUITER', 'HRMANAGER')
    ) UNIQUE
);


